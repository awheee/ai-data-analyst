# Local: copy to secrets.toml (never commit secrets.toml)
# Streamlit Cloud: paste into App Settings → Secrets (same keys)

GROQ_API_KEY = "gsk_your_key_here"

# Optional:
# GROQ_MODEL = "llama-3.1-8b-instant"
